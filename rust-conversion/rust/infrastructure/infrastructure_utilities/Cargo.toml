[package]
name = "infrastructure_utilities"
version = "0.1.0"
edition = "2021"
description = "Infrastructure layer: Utilities - common helper functions"
license = "Apache-2.0"
authors = ["Erlang/OTP Rust Conversion"]

[dependencies]
entities_data_handling = { path = "../../entities/entities_data_handling" }
entities_utilities = { path = "../../entities/entities_utilities" }
entities_process = { path = "../../entities/entities_process" }
entities_io_operations = { path = "../../entities/entities_io_operations" }
malachite = "0.7"
num-traits = "0.2"
num_cpus = "1.16"
flate2 = "1.0"
zstd = "0.13"
code_management_code_loading = { path = "../../code_management/code_management_code_loading" }
# Note: num-rational removed - now using BigRational from entities_utilities
# Note: ruzstd removed - using zstd crate for both compression and decompression
# Note: infrastructure_bif_dispatcher dependency removed to avoid circular dependency
#       BIF integration should be done at a higher layer (frameworks)

